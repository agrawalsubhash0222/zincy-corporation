# QA database configuration must be supplied by the QA environment.
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Temporary until database migrations are introduced.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# QA logging
logging.level.root=INFO
logging.level.com.zincycorporation=INFO

# QA frontend origin
app.frontend-url=${FRONTEND_URL}

# Correctly process proxy headers from Nginx.
server.forward-headers-strategy=framework

# Do not expose health details publicly.
management.endpoint.health.show-details=never
